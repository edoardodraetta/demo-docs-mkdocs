# .readthedocs.yaml
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: docs/requirements.txt
```

And a `docs/requirements.txt`:
```
mkdocs
mkdocs-material
mkdocstrings[python]
mkdocs-jupyter