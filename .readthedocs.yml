version: 2 # RTD yaml version

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: mypackage/.  # Installs the package so mkdocstrings can find it